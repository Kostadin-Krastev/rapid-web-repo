/*
purchased 3 cabinets. 
= print the total cost of the three cabinets =
One cabinet cost $n, 
the second was 20% cheaper than the first, 
the third cost 15% more than the other two combined.

The output must be rounded to the third decimal point. .toFixed(3)
 */

const calculateEachCabinetCost = function(inputPrice) {
let oneCabinetCost = inputPrice;
const secondCabinetCost = oneCabinetCost * 0.8;
const thirdCabinetCost = (oneCabinetCost + secondCabinetCost) * 1.15;
};

const calculateTotalCabinetPrice = function {
  
}